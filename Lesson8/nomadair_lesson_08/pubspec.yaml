name: nomadair_lesson_08
description: "NomadAir Lesson 08 — Inclusive Design and WCAG 2.1 Foundations"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.3.2
  nomadair_core:
    path: packages/core
  nomadair_ui:
    path: packages/ui

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
